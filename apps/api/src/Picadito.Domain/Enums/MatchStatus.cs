namespace Picadito.Domain.Enums;

/// <summary>
/// Estado de un partido: scheduled (programado), played (jugado), cancelled (cancelado).
/// </summary>
public enum MatchStatus
{
    scheduled,
    played,
    cancelled
}
