{
  "name": "picadito-monorepo",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev:web",
    "start": "npm run dev:web",
    "dev:web": "npm run dev --workspace=web",
    "build:web": "npm run build --workspace=web"
  },
  "dependencies": {
    "next": "15.5.8"
  }
}
